<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>METEO EN FRANCE</title>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.4/angular.min.js"></script>
    <script type="text/javascript" src="https://d3js.org/d3.v3.min.js"></script>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body ng-app="ekiMeteo">
    <div class="main" ng-controller="MainController">
      <!-- form -->
      <div class="container form">
        <div class="row">
          <div class="col-xs-12 col-sm-8 col-sm-offset-2">
            <form class="form-inline text-center">
              <input type="text" id="ville" class="form-control" name="city" ng-model="city" placeholder="Paris">
              <input type="text" id="datepicker" class="form-control" name="date" ng-model="date" placeholder="07/04/2011">
              <button type="submit" ng-click="updateData()" class="btn btn-search">rechercher</button>
            </form>
          </div>
        </div>
      </div>
      <!-- histogram of temperatures in D3 -->
      <div class="container marged-top">
        <div class="row">
        <div class="col-xs-12 col-sm-8 col-sm-offset-2 text-center">
          <h4>Évolution de la température à {{ city }}, le {{ date }}:</h4>
          <bar-chart height="350" data="data"></bar-chart>
        </div>
        </div>
      </div>

    </div>
  </body>
</html>
